# ~/.tmuxinator/Dotfiles.yml

name: printApp
root: ~/Code/printapp

windows:
  - Editor: nvim
  - Utils:
      layout: main-horizontal
      panes:
        - php artisan serve
        - npm run watch
  - File Manager: ranger
  - Remote: ssh apptest
